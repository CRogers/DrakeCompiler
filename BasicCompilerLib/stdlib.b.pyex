namespace System {

$for(x in [8,16,32,64])\
	class Int$(x) {
$for(binop in ["+", "-", "/", "*", "%", "&", "|"])\
		public static $(binop)(a: Int$(x), b: Int$(x)): Int$(x) {
			return 0;
		}
$endfor\
	}
$endfor\

	class Console {
		public static println(x: Int32) {
		}
	}

	class String {
		
	}
}